# 验证记录

## 0.2.0 · 自动生产批次（2026-09-25）

环境：macOS，Godot 4.7.2，Python 3.14，Node 24.14，tripo-cli 0.5.1。本地自动化验证与真实在线批次分开记录。

### 本地自动化验证

- **92 项 Python 测试通过**：24 项批次测试，加原有 68 项存储、服务、传输与 Tripo 适配器测试。实际输出为 `Ran 92 tests`、`OK`。
- 批次测试覆盖完整图预建、实际参考图顺序和输入版本、已有图片/节点复用、重复阶段去重、有界并发、独立失败分支、暂停/继续、重启后查询原任务、同时启动去重，以及不明确提交不重复购买。配方或上游变化会阻止批次偷偷使用新输入；创建失败会回滚节点及复制的产物。
- **35 项 Godot 批次 UI 测试通过**：隔离临时项目内检查空闲时持续同步、外部新增批次与完整节点、进度/错误、开始/暂停/继续请求、即时图片缩略图、视角和草稿保留，以及项目不匹配时停止操作。RPC 使用本地记录桩，不启动生产任务。
- **84 项 Godot 预览回归检查通过**：无窗口加载真实报刊亭 GLB 与参考图片，覆盖图内预览、版本切换、预览实例和视角保留、依赖资产及历史输入。该回归测试没有可见渲染检查，不沿用 0.1.2 可见测试的 92 项数字。
- 两组 Godot 日志分别输出 `ASSET_BATCH_UI_PROBE_PASS checks=35` 和 `ASSET_PREVIEW_PROBE_PASS checks=84`，无脚本或引擎错误。测试未上传图片、未提交付费生成请求，未修改用户场景。
- 配套 Skill 通过 `quick_validate` 格式检查。

重复运行：

```sh
python3 -m unittest discover -s tests -v
python3 tests/run_batch_ui_probe.py --godot /path/to/Godot
python3 tests/run_preview_probe.py --godot /path/to/Godot --assets /path/to/assets/tripo_urban
```

### 真实在线批次

- `sixth_street_core_v1` 达到 **completed，14 / 14**。一次 `tools/run_batch.py` 调用完成批次创建、已有产物导入和运行；后台自行查询供应商任务、下载模型并入库，中间没有手动推进阶段。
- 复用 12 个生产阶段，只新提交街机店和拉面店两个 P2 模型。API 返回的 `metadata.credits_consumed` 确认每个模型 **110 credits**，本批次新增 **220 credits**。街道底座此前生成消耗 110 credits，此次复用；三个模型累计消耗 **330 credits**，没有把复用计为新增购买。
- 在真实 Godot「资产工坊」中观察并截图记录了 **12 / 14**、街机店 **65%** / 拉面店 **67%**，随后进度到 **99%**。面板每 2 秒自动同步，没有手动刷新；最终完成状态与模型生成、下载和版本入库已核对。
- 本轮图片阶段复用内置生图工具已经生成的主体与独立视图，**没有重新调用在线 image-to-image 或 text-to-image 生成这些图片**。因此在线验证证明了已有图片驱动的批次调度、P2 模型生成和下载入库，不能视为从全新概念图开始的每个图片 API 阶段都已在线验证。

### 独立模型渲染检查

`tools/run_asset_review.py` 与 `tools/render_asset_review.gd` 在临时空项目中完成真实 GLB 检查，使用 Godot 4.7.2、OpenGL Compatibility 与 Apple M5。四份模型默认输出 12 张正交 PNG；添加 `--extra-views` 后输出正面、右侧、背面、俯视和三分之四方向，共 20 张 PNG。

- `review.json` 记录 **0 错误**，所有图片像素变化统计大于零；四份源 GLB 前后 SHA-256 一致。
- 四份模型均加载到 1 个网格实例、1 个材质。街机店、拉面店、街道底座各加载 3 张 2048 × 2048 贴图；历史报刊亭加载 1 张 4096 × 4096 贴图。
- 三角面数：街机店 **11,800**、拉面店 **13,792**、街道底座 **8,325**、历史报刊亭 **14,773**。报告同时保存原始包围盒和各方向观察参数。
- 这项检查证明产物能够导入和渲染，未对美术质量、语义朝向或真实世界尺寸作自动合格判定；未修改当前编辑器场景或源模型。

第三方游戏参考图、派生图片与模型、项目数据库和运行截图不包含在源码发布包中。完整 FBX 外部贴图包还原仍未实现。

## 0.1.2 · 右侧依赖资产

- 右侧以真实图片/模型展示直接上游资产及用途、版本，支持多产物切换和定位节点。
- 「当前依赖」读取当前配方和上游当前版本；「本版本使用」读取所选产物保存的输入快照。历史文件不可用时不会改用最新文件。
- 覆盖上游版本变化、连线移除/用途变化、历史输入为空、旧格式版本映射、节点缺失/无产物、版本切换与视角保持。定位节点有未保存内容保护。
- 同一真实资源测试套件包含原有图内预览测试与新增依赖测试，可见渲染 **92 项通过**，无脚本或引擎错误；没有调用付费 API。

## 0.1.1 · 图内图片与可旋转模型

- 真实 GLB 和图片的 Godot 可见渲染测试通过 **54 项**。实际视口图像具有非均匀像素，旋转前后图像内容确实改变。
- 鼠标事件由真实根视口派发；图内左键拖动旋转模型、不移动卡片，滚轮缩放模型、不缩放整张图。点击预览选中节点。
- 相同产物重复刷新保留模型实例、相机和缩放；修改版本只更新对应预览，旧版本可回退，多产物文件选择保留。
- 图片真实解码，模型网格实际加载；每个预览使用独立 World3D，停止交互后视口不持续渲染，删除节点会释放控件。
- 右侧详情优先展示 3D 模型，刷新保留其视角。未生成、文件缺失和不支持的格式均有明确状态。
- 原有 67 项 Python 测试继续通过。已安装到 SurfaceWorkshopDemo，并在原生「资产工坊」检查六个真实节点。

可重复运行（需自行提供两份 GLB 与参考图片，不随源码包分发）：

```sh
python3 tests/run_preview_probe.py --godot /path/to/Godot --assets /path/to/assets/tripo_urban --visible --output-dir /tmp/asset-preview-proof
```

测试文件：`tests/godot_preview_probe.gd`、`tests/run_preview_probe.py`。移除 `--visible` 可做无窗口的资源/状态验证，但不能替代真实渲染检查。未调用付费 API。

## 0.1.0 · 生产链与场景实例

环境：macOS，Godot 4.7.2，Python 3.14，Node 24.14，tripo-cli 0.5.1。目标依赖要求 Python 3.9+、Node 20+，其他操作系统尚未实机验证。

- 67 项 Python 单元/集成测试通过：SQLite 版本图、依赖失效、并发持久化、过期任务、暂停恢复、重复提交保护、提交失败分类、下载完整性、HTTP 鉴权、MCP 实际子进程。
- 官方 MCP JavaScript SDK 1.30.1 对 Python stdio 服务握手成功，发现 13 个工具，快照调用和错误关联通过。
- Godot 原生编辑器 24 项实例测试通过：首次放置、更新 GLB、owner 和版本元数据、保留包装节点变换及手动子节点、原生 Undo/Redo。测试实现见 `tests/godot_editor_probe.gd`，使用隔离场景。
- Godot 面板脚本解析、控件/节点/连线实例化通过；对外部 Python 进程的鉴权握手与断线刷新重连通过。
- 真实报刊亭历史已导入 SurfaceWorkshopDemo：6 个节点、7 个输出版本；右视图和背视图各保留主体图 + 正面图两个依赖。历史提示词保留全文。
- 当前主体、三个视图和 P2 模型的生成计划均通过官方接口构建校验。模型输入明确为 front/back/right，不依赖文件名或位置猜测。
- CLI 的真实请求构建器与客户端无重试行为使用离线请求桩验证；没有上传图片或提交新的付费生成任务。现有 GLB 来自之前真实 Studio 生成。

未验证：本次新增任务的完整在线计费/生成/下载链路，以及完整 FBX 外部贴图包还原。不能把上述离线测试理解为本次已成功生成新模型。
