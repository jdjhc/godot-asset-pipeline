# 展示素材

`asset-pipeline-showcase.gif`：1008 × 630，10 fps，约 14.4 秒，循环播放。

使用真实 Asset Pipeline 面板与已有建筑生成结果，在隔离项目里程序化回放并逐帧渲染；没有拍摄实时生成过程，没有修改用户项目。

展示模型用于工作流演示，参考《绝区零》街区设计；不将这些示例资产声明为插件原创美术或纳入插件代码许可。此目录不包含可下载的 3D 模型。
